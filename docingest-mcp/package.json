{
  "name": "docingest-mcp",
  "version": "0.1.0",
  "description": "Document-ingestion MCP server — retrieves authorized files from URLs/pages/connectors, detects type by signature, extracts text/tables into a normalized document, and returns concise, cited, size-limited results. Runs as an asset under orchestrator-mcp.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "docingest-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.10"
  },
  "engines": {
    "node": ">=18"
  }
}
