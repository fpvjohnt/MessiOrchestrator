{
  "name": "ghmonitor-mcp",
  "version": "0.1.0",
  "description": "GitHub delivery-monitoring MCP server — reports pass/fail/pending status for commits and PRs (Actions, required checks, deployments, code scanning), summarizes failed-run logs into an actionable diagnosis, and dedupes to state-changes only. Runs as an asset under orchestrator-mcp.",
  "type": "module",
  "main": "dist/index.js",
  "bin": { "ghmonitor-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.10"
  },
  "engines": { "node": ">=18" }
}
